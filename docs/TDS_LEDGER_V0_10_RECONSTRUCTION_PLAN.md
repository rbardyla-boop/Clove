# TDS v0.10 Reconstruction Plan

Status: `RECONSTRUCTION_REQUIRED` if the exact file cannot be attached or
recovered. This plan does not create or publish a reconstructed ledger.

## Provenance boundary

The last verified ancestor is the vault file:

`Obsidian Vault/00_Inbox/EVIDENCE_CONSOLIDATION_LEDGER_v0.9.md`

SHA-256:
`b2dd2052a9dcfc5f1b9f15b0815ae92d2c6c5eef5484a1fd765345e6054ab408`

An output assembled from v0.9 plus later research cannot be called the lost
historical v0.10. If created, it must be named:

`EVIDENCE_CONSOLIDATION_LEDGER_v0.10-RECONSTRUCTED.md`

and begin with:

`STATUS: RECONSTRUCTED CANDIDATE — NOT SOURCE-IDENTICAL TO THE LOST/MISSING v0.10`

## Required reconstruction inputs

- v0.9 full text and SHA above.
- The 35-row `MYTHS_WE_KILLED_AND_SURVIVING_THEORY_MERGE_GATE_v0.1.md`.
- The algorithm, microtargeting, WEF, Canada privacy, social-proof,
  Trump-stress-test, and empirical-gauntlet research windows listed in the
  post-v0.9 delta report.
- Any attached File Library copy of v0.10, if recovered later; this must win
  over reconstruction and be hashed before use.

## Required delta audit

Before a reconstructed candidate could unlock public generation, an
independent human audit must compare:

1. every v0.9 EC claim and raw qualifier;
2. the three unresolved v0.9 status records: EC-046, EC-051A, EC-086;
3. additions, removals, and supersessions claim by claim;
4. every DO_NOT_RESURRECT / MK item and its replacement theory;
5. source URLs, DOI identifiers, denominators, sample sizes, effect sizes,
   jurisdictions, temporal qualifiers, and CANNOT CLAIM boundaries;
6. PUBLIC / CAUTION / HOLD classification for every source and dossier;
7. whether a sentence is a proposed experiment or a completed result.

No reconstructed claim may be upgraded merely because it appears in a later
research plan or because a source URL resolves.

## Required gates

The reconstructed candidate remains blocked until all of the following are
true:

- every claim has exactly one frozen evidence status;
- every claim has GREEN/YELLOW/RED audit state;
- unresolved status wording is represented in qualifier fields, not erased;
- source identity and source role are explicit;
- no broken internal citation token reaches public output;
- public/hold firewall passes;
- independent human reconciliation signs off the delta;
- the existing deterministic builder passes without weakening validation.

Until then, `TDS_LEDGER_SOURCE_PACKET_MISSING` remains the correct public-build
state and no `/research/projects/tds/ledger/` output may be deployed.

## Next single action

Attach the exact File Library artifact to Codex, or explicitly authorize a
separate reconstructed-candidate workflow. The exact artifact is preferred;
reconstruction cannot establish the provenance of a lost file.
